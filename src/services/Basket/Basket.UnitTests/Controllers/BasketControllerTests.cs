using Basket.Controllers;
using Basket.Repositories;
using NUnit.Framework;
using System;
using System.Threading.Tasks;

namespace Basket.UnitTests.Controllers
{
    [TestFixture]
    public class BasketControllerTests
    {
        /*
        [Test]
        public void GetBasket_NonExistentBasket_ReturnsEmptyArray()
        {
            var sut = new Calculator();
            int result = sut.AddNumbers(1, 2);
            Assert.That(result, Is.EqualTo(3));
        }

        [Test]
        public async Task GetBasket_StateUnderTest_ExpectedBehavior()
        {
            // Arrange
            var basketController = new BasketController(TODO);
            string userName = null;

            // Act
            var result = await basketController.GetBasket(
                userName);

            // Assert
            Assert.Fail();
        }

        [Test]
        public async Task UpdateBasket_StateUnderTest_ExpectedBehavior()
        {
            // Arrange
            var basketController = new BasketController(TODO);
            ShoppingCart basket = null;

            // Act
            var result = await basketController.UpdateBasket(
                basket);

            // Assert
            Assert.Fail();
        }

        [Test]
        public async Task DeleteBasket_StateUnderTest_ExpectedBehavior()
        {
            // Arrange
            var basketController = new BasketController(TODO);
            string userName = null;

            // Act
            var result = await basketController.DeleteBasket(
                userName);

            // Assert
            Assert.Fail();
        }
        */
    }
}
